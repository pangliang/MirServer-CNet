namespace GameFramework
{
    public unsafe static class SByteExtension
    {
        //public unsafe static string ToString(this TStdItem StdItem, int Len)//扩建的方法必须是静态方法，参数里面必须含有this关键字，this关键字后面的类型为需要扩展的类型
        //{
        //    return HUtil32.SBytePtrToString(HUtil32.SBytePtrToString(StdItem->Name, StdItem->NameLen), Len);
        //}
    }
}